apiVersion: 1
datasources:
  - name: kibot-api
    type: yesoreyeram-infinity-datasource
    access: proxy
    url: http://localhost:8787
    editable: true
    jsonData:
      datasource_mode: "basic"
