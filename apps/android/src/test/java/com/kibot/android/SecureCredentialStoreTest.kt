package com.kibot.android

import kotlin.test.Test
import kotlin.test.assertTrue

class SecureCredentialStoreTest {
    @Test
    fun `placeholder test keeps android module wired for unit tests`() {
        assertTrue(true)
    }
}
