# ── Build & IDE ──────────────────────────────────────────────
.gradle/
build/
**/build/
local.properties
.DS_Store
.idea/
*.iml
__pycache__/
*.pyc
captures/
.kotlin/
*.hprof
*.keystore
*.jks
*.db
*.db-shm
*.db-wal

# ── Secrets & Credentials (NEVER commit) ────────────────────
.env
.env.*
!.env.example
.secrets/
*.credentials.md
*.pem
*.key
!gradlew

# ── SSH Keys (block all patterns) ───────────────────────────
SSH_BINANCE/
SSH_INDODAX/
SSH_BATAM/*.pem
SSH_BATAM/*.pub
SSH_BATAM/*.key
!SSH_BATAM/README.md
SSH_SINGAPORE/**/*.key
SSH_SINGAPORE/**/*.key.pub
!SSH_SINGAPORE/README.md
SSH_MANAGEMENT/
SSH_EXECUTOR*/
SSH_SCANNER/

# ── Virtual Environments (NEVER commit) ─────────────────────
.venv/
.venv-*/
venv/
.brain-venv/
.deploy-venv/
.tmp-venv/
.codex-venv/
*-venv/

# ── Node.js (NEVER commit) ─────────────────────────────────
node_modules/
npm-cache/

# ── Build Artifacts & Logs ──────────────────────────────────
*.jar
*.tar.gz
build_log.txt
build-output.txt
logs.zip
.tmp-kidax-last6m.log

# ── Deploy & Tooling ────────────────────────────────────────
.deploy/
.dist/
.tooling/
.continue/
.wrapper-bootstrap/
**/bin/

# ── State & Runtime ─────────────────────────────────────────
.state/
state/
!state/.gitkeep
simulation_report.json
*.tmp.*

# ── App-specific ────────────────────────────────────────────
apps/mac-engine/.env
apps/mac-engine/bin/
infra/supabase/.temp/
infra/supabase/.branches/
infra/supabase/.generated/
.tmp/

# ── Docs with generated/sensitive content ───────────────────
SUPABASE_MIGRATION_GUIDE.md
*_GUIDE.md

# ── Gradle ──────────────────────────────────────────────────
gradle_home/
.gradle_home/
.gradle-local/

# ── Logs (allow OPS_UPDATE_LOG.md only) ─────────────────────
logs/
!logs/
!logs/OPS_UPDATE_LOG.md

# ── OCI / Cloud ─────────────────────────────────────────────
.oci/

# ── Legacy scripts (already committed, mark for cleanup) ────
push_fix.sh
